name: Build A05m Kernel

on:
  workflow_dispatch:
    inputs:
      root_provider:
        description: "Solución de root"
        type: choice
        options:
          - ksu-next-stable
          - sukisu-ultra-v4.1.3
        default: ksu-next-stable

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Liberar espacio en el runner
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache
          sudo rm -rf /usr/local/lib/android/sdk/system-images \
                      /usr/local/lib/android/sdk/emulator
          sudo apt-get clean
          df -h

      - name: Instalar dependencias
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc bison flex libssl-dev \
            libncurses-dev python3 python3-pip zip unzip rsync kmod cpio \
            device-tree-compiler default-jdk git curl patch

      - name: Descargar toolchain (con reintentos)
        run: |
          pip install gdown
          for i in 1 2 3; do
            gdown "https://drive.google.com/uc?id=1Nq0bm5VMJKt32RRV0_AzBPvpH6YflE5F" -O toolchain.tar.gz && break
            echo "Intento $i falló, reintentando en 10s..."
            sleep 10
          done
          test -s toolchain.tar.gz || { echo "❌ No se pudo descargar el toolchain"; exit 1; }
          tar -xf toolchain.tar.gz

      - name: Neutralizar setup.sh original en build_kernel.sh
        run: |
          sed -i '/rsuntk\/KernelSU\/main\/kernel\/setup.sh/d' build_kernel.sh

      - name: Integrar KernelSU-Next (tag v3.2.0)
        if: ${{ inputs.root_provider == 'ksu-next-stable' }}
        working-directory: ./kernel-6.6
        run: |
          curl -LSs "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/refs/heads/dev/kernel/setup.sh" | bash -s v3.2.0
          test -d KernelSU-Next || { echo "❌ Falló la integración de KernelSU-Next"; exit 1; }
          echo "KSUN_COMMIT=$(cd KernelSU-Next && git rev-parse HEAD)" >> "$GITHUB_ENV"

      - name: Setup Java (KSUN)
        if: ${{ inputs.root_provider == 'ksu-next-stable' }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Compilar manager de KernelSU-Next
        if: ${{ inputs.root_provider == 'ksu-next-stable' }}
        run: |
          git clone https://github.com/KernelSU-Next/KernelSU-Next.git ksun-manager-src
          cd ksun-manager-src
          git checkout "$KSUN_COMMIT"
          cd manager
          echo "sdk.dir=$ANDROID_HOME" > local.properties
          chmod +x gradlew
          ./gradlew assembleRelease
          find . -name "*.apk"

      - name: Subir manager APK (KSUN)
        if: ${{ inputs.root_provider == 'ksu-next-stable' }}
        uses: actions/upload-artifact@v4
        with:
          name: ksun-manager-${{ github.run_number }}
          path: ksun-manager-src/manager/**/*.apk
          retention-days: 14

      # --- INICIO CORRECCIONES SUKISU Y SUSFS ---

      - name: Integrar SukiSU-Ultra (Fijado a tag v4.1.3 estable)
        if: ${{ inputs.root_provider == 'sukisu-ultra-v4.1.3' }}
        working-directory: ./kernel-6.6
        run: |
          # Cambiamos susfs-main por v4.1.3 para garantizar estabilidad
          curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s v4.1.3
          echo "SUKISU_COMMIT=$(cd KernelSU && git rev-parse HEAD)" >> "$GITHUB_ENV"

      - name: Integrar parches SUSFS al código del Kernel (Adaptado para 6.6)
        if: ${{ inputs.root_provider == 'sukisu-ultra-v4.1.3' }}
        working-directory: ./kernel-6.6
        run: |
          echo "📥 Clonando repositorio de SUSFS..."
          git clone https://gitlab.com/simonpunk/susfs4ksu.git ../susfs4ksu
          
          echo "📂 Copiando archivos de SUSFS al kernel..."
          cp -r ../susfs4ksu/kernel_patches/fs/* ./fs/
          cp -r ../susfs4ksu/kernel_patches/include/linux/* ./include/linux/
          
          echo "🩹 Aplicando parches de SUSFS al kernel..."
          PATCH_DIR="../susfs4ksu/kernel_patches/kernel_6.1"
          
          # Aplicamos ignorando pequeños desajustes (típico al portar de 6.1 a 6.6)
          patch -p1 --forward < $PATCH_DIR/50_add_susfs_in_kernel-6.1.patch || echo "⚠️ Revisa posibles conflictos menores en el parche 50"
          patch -p1 --forward < $PATCH_DIR/51_susfs_add_hook_in_kernel-6.1.patch || echo "⚠️ Revisa posibles conflictos menores en el parche 51"
          
          # HACK VITAL PARA KERNEL 6.6: 
          # Asegurarnos a la fuerza de que el kernel sepa que tiene que compilar susfs.o
          if ! grep -q "susfs" fs/Makefile; then
            echo "Inyectando susfs.o en fs/Makefile manualmente porque el parche falló..."
            echo "obj-\$(CONFIG_KSU_SUSFS) += susfs.o" >> fs/Makefile
          fi

      - name: Setup Java (SukiSU)
        if: ${{ inputs.root_provider == 'sukisu-ultra-v4.1.3' }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Compilar manager de SukiSU-Ultra
        if: ${{ inputs.root_provider == 'sukisu-ultra-v4.1.3' }}
        run: |
          git clone https://github.com/SukiSU-Ultra/SukiSU-Ultra.git suki-manager-src
          cd suki-manager-src
          # Como ahora apuntamos a la v4.1.3, este APK será la versión estable
          git checkout "$SUKISU_COMMIT"
          cd manager
          echo "sdk.dir=$ANDROID_HOME" > local.properties
          chmod +x gradlew
          ./gradlew assembleRelease
          find . -name "*.apk"

      - name: Subir manager APK (SukiSU)
        if: ${{ inputs.root_provider == 'sukisu-ultra-v4.1.3' }}
        uses: actions/upload-artifact@v4
        with:
          name: sukisu-manager-${{ github.run_number }}
          path: suki-manager-src/manager/**/*.apk
          retention-days: 14

      - name: Habilitar CONFIG_KSU y Bloque Completo SUSFS
        run: |
          # Encontramos la configuración e inyectamos TODAS las flags de SUSFS
          CFG=$(grep -rl "CONFIG_KSU" --include="*.config" . 2>/dev/null | head -n1)
          if [ -z "$CFG" ]; then
            CFG="./kernel/kernel_device_modules-6.6/kernel/configs/mt6768_overlay.config"
            echo "⚠️ No se encontró config existente, usando default: $CFG"
          fi
          echo "Usando config: $CFG"
          
          # Habilitar KSU base
          if ! grep -q "CONFIG_KSU=y" "$CFG"; then
            echo "CONFIG_KSU=y" >> "$CFG"
          fi
          
          # Inyectar el bloque entero de SUSFS
          if [ "${{ inputs.root_provider }}" == "sukisu-ultra-v4.1.3" ]; then
            echo "Inyectando flags completas de SUSFS..."
            # Eliminamos las flags si existieran para evitar duplicados, y las insertamos limpias
            sed -i '/CONFIG_KSU_SUSFS/d' "$CFG"
            cat <<EOF >> "$CFG"
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          EOF
          fi

      # --- FIN CORRECCIONES ---

      - name: Cache de Bazel
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/bazel
            ~/.cache/bazelisk
          key: bazel-cache-${{ runner.os }}-${{ inputs.root_provider }}-${{ hashFiles('kernel/WORKSPACE') }}

      - name: Compilar
        run: |
          chmod +x build_kernel.sh
          bash build_kernel.sh

      - name: Verificar que se generó el zip
        run: |
          ls -la *.zip || { echo "❌ No se generó ningún zip, revisar log de Compilar"; exit 1; }

      - name: Subir artifact
        uses: actions/upload-artifact@v4
        with:
          name: a05m-kernel-${{ inputs.root_provider }}-${{ github.run_number }}
          path: "*.zip"
          retention-days: 14
